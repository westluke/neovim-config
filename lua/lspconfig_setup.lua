-- I THINK THIS IS WRONG, THATS WHY NOTHING IS ACTIVATING
return {
    "neovim/nvim-lspconfig", -- REQUIRED: for native Neovim LSP integration
    lazy = false, -- REQUIRED: tell lazy.nvim to start this plugin at startup

    init = function()
    end,

    config = function()
    end,
}
